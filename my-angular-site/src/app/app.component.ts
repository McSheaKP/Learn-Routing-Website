import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //name of the tag that the component is applied to
  templateUrl: './app.component.html', // defines html for the component
  styleUrls: ['./app.component.css'] //defines html for this component
})
export class AppComponent {
  title = "Kevin's Projects Page";
}
